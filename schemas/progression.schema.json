{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Progression Schema",
  "description": "Defines level thresholds and stat growth curves for player progression.",
  "type": "object",
  "properties": {
    "level_thresholds": {
      "type": "array",
      "items": { "type": "integer", "minimum": 0 },
      "description": "Cumulative XP required to reach each level. Index 0 is level 2, index 1 is level 3, etc."
    },
    "stat_growth": {
      "type": "object",
      "patternProperties": {
        "^[0-9]+$": {
          "type": "object",
        "properties": {
          "hp_max": { "type": "integer", "minimum": 0 },
          "off": { "type": "integer", "minimum": 0 },
          "def": { "type": "integer", "minimum": 0 },
          "spd": { "type": "integer", "minimum": 0 }
        },
          "required": ["hp_max", "off", "def", "spd"]
        }
      },
      "additionalProperties": false,
      "description": "Stat increases awarded when reaching a specific level. Key is the level reached (as string)."
    }
  },
  "required": ["level_thresholds", "stat_growth"]
}
