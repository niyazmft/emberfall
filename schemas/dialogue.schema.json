{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dialogue Registry Schema",
  "type": "object",
  "description": "A collection of dialogue entries keyed by unique ID.",
  "additionalProperties": {
    "type": "object",
    "required": ["text"],
    "properties": {
      "text": {
        "type": "string",
        "description": "The raw text or template string."
      },
      "localization_key": {
        "type": "string",
        "description": "Key for looking up localized text."
      },
      "next_id": {
        "type": "string",
        "description": "The ID of the next dialogue entry in a sequence."
      },
      "metadata": {
        "type": "object",
        "description": "Arbitrary metadata associated with this entry (e.g., emotions, timing, conditions)."
      }
    }
  }
}
